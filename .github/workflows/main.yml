name: tests
on:
  pull_request:
    branches:
    - master
jobs:
  test_workflow:
    runs-on: ubuntu-latest
    name: Test workflow with docker run 
    steps:
      - uses: actions/checkout@v3
      - name: Prepare
        run: |
          mkdir outputs
      # run the container with the script in the repository
      - name: Going to a container
        id: container
        run: | 
          chmod +x commands.sh
          mkdir shared
          chmod 777 shared
          mv commands.sh shared
          docker run --rm -v ${PWD}/shared:/code  gitlab-registry.cern.ch/cms-cloud/cmssw-docker/cmssw_10_6_30-slc7_amd64_gcc700 bash -c /code/commands.sh
          echo ls -l  `ls -l `
          cp shared/output/* outputs
      # upload output file
      - name: Upload artifact
        uses: actions/upload-artifact@v2
        with:
          name: output
          path: outputs/
